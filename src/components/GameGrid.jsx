import { Grid } from "@mui/material";

export default function GameGrid() {

    return (
        <Grid container spacing={0} columns={7}>
            <Grid item>
                
            </Grid>
            <Grid item>

            </Grid>

        </Grid>
    )
}